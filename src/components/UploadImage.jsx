import { Button, Group } from "@mantine/core";

import { useForm } from "@mantine/form";

function UploadImage({ setFields, nextStep, prevStep }) {
  const form = useForm
 

  console.log("fields:", fields)

  return (
    
  );
}

export default UploadImage;
